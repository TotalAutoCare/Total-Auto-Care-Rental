
{
  "name": "tac-rental-finance",
  "version": "1.0.0",
  "private": true,
  "description": "Personal Finance Architect - Vercel Ready",
  "type": "module",
  "dependencies": {
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "recharts": "^3.6.0",
    "@google/genai": "^1.35.0"
  },
  "scripts": {
    "start": "npx serve .",
    "build": "rm -rf dist && mkdir -p dist/components dist/services && cp ./*.html ./*.tsx ./*.ts ./*.json ./*.png dist/ && cp ./components/*.tsx dist/components/ && cp ./services/*.ts dist/services/ || true",
    "dev": "npx serve ."
  }
}
