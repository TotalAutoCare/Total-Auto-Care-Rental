{
  "name": "tac-rental-finance",
  "version": "1.0.0",
  "private": true,
  "description": "Personal Finance Architect - Vercel Ready",
  "type": "module",
  "dependencies": {
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "recharts": "^3.6.0",
    "@google/genai": "^1.35.0"
  },
  "scripts": {
    "start": "npx serve .",
    "build": "mkdir -p dist && cp -rv ./*.html ./*.tsx ./*.ts ./*.json ./*.png ./components ./services dist/ || true",
    "dev": "npx serve ."
  }
}